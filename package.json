{
  "name": "epoch-base-polygon",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ epoch-protocol/epoch-base-polygon",
    "create-local": "graph create --node http://localhost:8020/ epoch-protocol/epoch-base-polygon",
    "remove-local": "graph remove --node http://localhost:8020/ epoch-protocol/epoch-base-polygon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 epoch-protocol/epoch-base-polygon"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.33.1",
    "@graphprotocol/graph-ts": "^0.27.0"
  }
}
